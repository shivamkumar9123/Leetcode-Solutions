class Solution {
public:
    int fib(int n) {
        if(n<=1)
        return n;
        // return fib(n-1)+fib(n-2);
        int m=0;
        int p=1;
        for(int i=2;i<=n;i++){
           int curr=m+p;
           m=p;
           p=curr;
        }
        return p;
    }
};
