class Solution {
public:
    bool isIsomorphic(string s, string t) {
        // BruteForce Approach
    //    if(s.length()!= t.length()){
    //     return false;
    //    }
    //    int n=s.length();
    //    for(int i=0; i<n;i++){
    //     for(int j=0;j<i;j++){
    //         if(s[i] == s[j] && t[i]!=t[j]){
    //             return false;
    //         }
    //         if(t[i]==t[j] && s[i]!=s[j]){
    //             return false;
    //         }
    //     }
    //    }
    //    return true;
    char map_s[128]={0};
    char map_t[128]={0};
    int m=s.size();
    for(int i=0;i<m;i++){
        if(map_s[s[i]] !=map_t[t[i]])
        return false;
        map_s[s[i]]=i+1;
        map_t[t[i]]=i+1;
    }
    return true;

    }
};
